name: 🚀 Repository Manager - Bulk App Updates

on:
  workflow_dispatch:
    inputs:
      update_config:
        description: "App Update Configuration (JSON)"
        required: false
        default: '{
          "spotify": {
            "update": false,
            "version": "9.0.91",
            "tweak_version": "v6.2",
            "download_url": "https://github.com/OofMini/eeveespotifyreborn/releases/download/New/EeveeSpotify.ipa"
          },
          "youtube": {
            "update": false,
            "version": "20.44.2", 
            "tweak_version": "v5.2b4",
            "download_url": "https://github.com/OofMini/YTLite/releases/download/New/YouTubePlus_5.2b4.ipa"
          },
          "twitter": {
            "update": false,
            "version": "11.36",
            "tweak_version": "v5.2",
            "download_url": "https://github.com/OofMini/tweak/releases/download/New/NeoFreeBird-sideloaded.ipa"
          }
        }'
      change_description:
        description: "Custom commit message (optional)"
        default: ""

jobs:
  repository-sync:
    name: "🔄 Sync App Repository"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout repository"
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔍 Validate JSON files"
        run: |
          echo "Validating JSON file structure..."
          for file in sidestore.json feather.json trollapps.json; do
            if [ -f "$file" ]; then
              if jq empty "$file" 2>/dev/null; then
                echo "✅ $file - Valid JSON"
              else
                echo "❌ $file - Invalid JSON"
                exit 1
              fi
            else
              echo "❌ $file - File not found"
              exit 1
            fi
          done

      - name: "📋 Parse update configuration"
        id: parse-config
        run: |
          echo "Parsing update configuration..."
          CONFIG='${{ inputs.update_config }}'
          
          # Validate the JSON configuration
          if ! echo "$CONFIG" | jq empty 2>/dev/null; then
            echo "❌ Invalid JSON configuration"
            exit 1
          fi
          
          # Extract values for each app
          SPOTIFY_UPDATE=$(echo "$CONFIG" | jq -r '.spotify.update')
          SPOTIFY_VERSION=$(echo "$CONFIG" | jq -r '.spotify.version')
          SPOTIFY_TWEAK=$(echo "$CONFIG" | jq -r '.spotify.tweak_version')
          SPOTIFY_URL=$(echo "$CONFIG" | jq -r '.spotify.download_url')
          
          YOUTUBE_UPDATE=$(echo "$CONFIG" | jq -r '.youtube.update')
          YOUTUBE_VERSION=$(echo "$CONFIG" | jq -r '.youtube.version')
          YOUTUBE_TWEAK=$(echo "$CONFIG" | jq -r '.youtube.tweak_version')
          YOUTUBE_URL=$(echo "$CONFIG" | jq -r '.youtube.download_url')
          
          TWITTER_UPDATE=$(echo "$CONFIG" | jq -r '.twitter.update')
          TWITTER_VERSION=$(echo "$CONFIG" | jq -r '.twitter.version')
          TWITTER_TWEAK=$(echo "$CONFIG" | jq -r '.twitter.tweak_version')
          TWITTER_URL=$(echo "$CONFIG" | jq -r '.twitter.download_url')
          
          # Set outputs
          echo "spotify_update=$SPOTIFY_UPDATE" >> $GITHUB_OUTPUT
          echo "spotify_version=$SPOTIFY_VERSION" >> $GITHUB_OUTPUT
          echo "spotify_tweak=$SPOTIFY_TWEAK" >> $GITHUB_OUTPUT
          echo "spotify_url=$SPOTIFY_URL" >> $GITHUB_OUTPUT
          
          echo "youtube_update=$YOUTUBE_UPDATE" >> $GITHUB_OUTPUT
          echo "youtube_version=$YOUTUBE_VERSION" >> $GITHUB_OUTPUT
          echo "youtube_tweak=$YOUTUBE_TWEAK" >> $GITHUB_OUTPUT
          echo "youtube_url=$YOUTUBE_URL" >> $GITHUB_OUTPUT
          
          echo "twitter_update=$TWITTER_UPDATE" >> $GITHUB_OUTPUT
          echo "twitter_version=$TWITTER_VERSION" >> $GITHUB_OUTPUT
          echo "twitter_tweak=$TWITTER_TWEAK" >> $GITHUB_OUTPUT
          echo "twitter_url=$TWITTER_URL" >> $GITHUB_OUTPUT
          
          echo "✅ Configuration parsed successfully"

      - name: "🔄 Update repository files"
        id: update-files
        run: |
          CURRENT_DATE=$(date -u +"%Y-%m-%d")
          echo "current_date=$CURRENT_DATE" >> $GITHUB_OUTPUT
          
          # Function to update app in JSON file
          update_app_in_file() {
            local file=$1
            local app_name=$2
            local bundle_id=$3
            local version=$4
            local download_url=$5
            local tweak_version=$6
            
            echo "Updating $app_name in $file..."
            
            # Generate localized description with updated versions
            if [[ "$app_name" == "EeveeSpotify" ]]; then
              LOCALIZED_DESC="Tweaked Spotify with premium features unlocked, no ads, and enhanced playback. Spotify v$version, EeveeSpotify $tweak_version."
            elif [[ "$app_name" == "YTLite" ]]; then
              LOCALIZED_DESC="Tweaked YouTube with background playback, no ads, and picture-in-picture. Youtube v$version, YTLite $tweak_version."
            elif [[ "$app_name" == "X (NeoFreeBird)" ]]; then
              LOCALIZED_DESC="Tweaked Twitter/X with premium features and more. X v$version, NeoFreeBird $tweak_version."
            else
              # For apps without version numbers in description, keep original text
              LOCALIZED_DESC=$(jq -r --arg app "$app_name" --arg bundle "$bundle_id" '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)).localizedDescription' "$file")
            fi
            
            # Update based on file structure
            if [[ "$file" == "sidestore.json" ]]; then
              jq --arg app "$app_name" \
                 --arg bundle "$bundle_id" \
                 --arg version "$version" \
                 --arg url "$download_url" \
                 --arg desc "$LOCALIZED_DESC" \
                 --arg date "$CURRENT_DATE" \
                 '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)) |= 
                  .versions[0].version = $version |
                  .versions[0].downloadURL = $url |
                  .versions[0].date = $date |
                  .localizedDescription = $desc' "$file" > "${file}.tmp"
                  
            elif [[ "$file" == "feather.json" ]]; then
              jq --arg app "$app_name" \
                 --arg bundle "$bundle_id" \
                 --arg version "$version" \
                 --arg url "$download_url" \
                 --arg desc "$LOCALIZED_DESC" \
                 --arg date "$CURRENT_DATE" \
                 '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)) |= 
                  .version = $version |
                  .downloadURL = $url |
                  .versionDate = $date |
                  .size = (.versions[0].size) |
                  .versions[0].version = $version |
                  .versions[0].downloadURL = $url |
                  .versions[0].date = $date |
                  .localizedDescription = $desc' "$file" > "${file}.tmp"
                  
            else  # trollapps.json
              jq --arg app "$app_name" \
                 --arg bundle "$bundle_id" \
                 --arg version "$version" \
                 --arg url "$download_url" \
                 --arg desc "$LOCALIZED_DESC" \
                 '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)) |= 
                  .version = $version |
                  .downloadURL = $url |
                  .localizedDescription = $desc' "$file" > "${file}.tmp"
            fi
            
            mv "${file}.tmp" "$file"
          }

          UPDATED_APPS=()
          
          # Update apps based on toggles from parsed config
          [[ "${{ steps.parse-config.outputs.spotify_update }}" == "true" ]] && UPDATED_APPS+=("EeveeSpotify") && for file in sidestore.json feather.json trollapps.json; do
            update_app_in_file "$file" "EeveeSpotify" "com.spotify.client" \
              "${{ steps.parse-config.outputs.spotify_version }}" \
              "${{ steps.parse-config.outputs.spotify_url }}" \
              "${{ steps.parse-config.outputs.spotify_tweak }}"
          done

          [[ "${{ steps.parse-config.outputs.youtube_update }}" == "true" ]] && UPDATED_APPS+=("YTLite") && for file in sidestore.json feather.json trollapps.json; do
            update_app_in_file "$file" "YTLite" "com.google.ios.youtube" \
              "${{ steps.parse-config.outputs.youtube_version }}" \
              "${{ steps.parse-config.outputs.youtube_url }}" \
              "${{ steps.parse-config.outputs.youtube_tweak }}"
          done

          [[ "${{ steps.parse-config.outputs.twitter_update }}" == "true" ]] && UPDATED_APPS+=("X (NeoFreeBird)") && for file in sidestore.json feather.json trollapps.json; do
            update_app_in_file "$file" "X (NeoFreeBird)" "com.atebits.Tweetie2" \
              "${{ steps.parse-config.outputs.twitter_version }}" \
              "${{ steps.parse-config.outputs.twitter_url }}" \
              "${{ steps.parse-config.outputs.twitter_tweak }}"
          done

          echo "updated_apps=${UPDATED_APPS[*]}" >> $GITHUB_OUTPUT

      - name: "✅ Verify changes"
        run: |
          echo "Verifying updated files..."
          for file in sidestore.json feather.json trollapps.json; do
            if jq empty "$file" 2>/dev/null; then
              echo "✅ $file - Still valid after updates"
            else
              echo "❌ $file - Became invalid after updates"
              exit 1
            fi
          done

      - name: "📝 Commit and push changes"
        if: steps.update-files.outputs.updated_apps
        run: |
          # Generate commit message
          if [[ -n "${{ inputs.change_description }}" ]]; then
            COMMIT_MSG="${{ inputs.change_description }}"
          else
            COMMIT_MSG="🔄 Repository Sync: Updated app versions across all sources"
            
            UPDATED_APPS_ARR=(${UPDATED_APPS[@]})
            if [ ${#UPDATED_APPS_ARR[@]} -gt 0 ]; then
              COMMIT_MSG+="\n\nUpdated apps:"
              for app in "${UPDATED_APPS_ARR[@]}"; do
                COMMIT_MSG+="\n• $app"
              done
            fi
            
            COMMIT_MSG+="\n\n🤖 Automated update via GitHub Actions"
          fi
          
          # Configure git and commit
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add sidestore.json feather.json trollapps.json
          
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "$(echo -e "$COMMIT_MSG")"
            git push
            echo "✅ Changes committed and pushed successfully"
          fi

      - name: "📊 Update summary"
        run: |
          echo "## 🎯 Repository Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📁 Files Updated:" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ sidestore.json" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ feather.json" >> $GITHUB_STEP_SUMMARY  
          echo "- ✅ trollapps.json" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -n "${{ steps.update-files.outputs.updated_apps }}" ]; then
            echo "### 📱 Apps Updated:" >> $GITHUB_STEP_SUMMARY
            for app in ${{ steps.update-files.outputs.updated_apps }}; do
              echo "- 🎯 $app" >> $GITHUB_STEP_SUMMARY
            done
          else
            echo "### ℹ️ No apps were updated (all toggles were off)" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Automated repository management* 🤖" >> $GITHUB_STEP_SUMMARY