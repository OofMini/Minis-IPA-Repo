name: 🚀 Repository Manager - Bulk App Updates

on:
  workflow_dispatch:
    inputs:
      # Consolidated app selection
      apps_to_update:
        description: "📱 Apps to Update (comma-separated)"
        required: true
        default: "spotify,youtube,twitter"
        type: choice
        options:
        - spotify
        - youtube  
        - twitter
        - spotify,youtube
        - spotify,twitter
        - youtube,twitter
        - spotify,youtube,twitter

      # Consolidated version inputs
      spotify_version:
        description: "🎵 Spotify v/Tweak v (format: 9.0.91/v6.2)"
        default: "9.0.91/v6.2"
      spotify_url:
        description: "🎵 Spotify Download URL"
        default: "https://github.com/OofMini/eeveespotifyreborn/releases/download/New/EeveeSpotify.ipa"
      
      youtube_version:
        description: "📺 YouTube v/Tweak v (format: 20.44.2/v5.2b4)"
        default: "20.44.2/v5.2b4"
      youtube_url:
        description: "📺 YouTube Download URL"
        default: "https://github.com/OofMini/YTLite/releases/download/New/YouTubePlus_5.2b4.ipa"
      
      twitter_version:
        description: "🐦 Twitter v/Tweak v (format: 11.36/v5.2)"
        default: "11.36/v5.2"
      twitter_url:
        description: "🐦 Twitter Download URL"
        default: "https://github.com/OofMini/tweak/releases/download/New/NeoFreeBird-sideloaded.ipa"
      
      # Options
      change_description:
        description: "📝 Custom commit message (optional)"
        default: ""

jobs:
  repository-sync:
    name: "🔄 Sync App Repository"
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout repository"
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔄 Update repository files"
        id: update-files
        run: |
          set -e
          CURRENT_DATE=$(date -u +"%Y-%m-%d")
          echo "current_date=$CURRENT_DATE" >> $GITHUB_OUTPUT
          
          # Parse version strings (app_version/tweak_version)
          parse_version() {
            local version_string=$1
            echo "$version_string" | cut -d'/' -f1
          }
          
          parse_tweak_version() {
            local version_string=$1
            echo "$version_string" | cut -d'/' -f2
          }
          
          # Function to validate JSON files
          validate_json() {
            echo "🔍 Validating JSON file structure..."
            for file in sidestore.json feather.json trollapps.json; do
              if [ -f "$file" ]; then
                if jq empty "$file" 2>/dev/null; then
                  echo "✅ $file - Valid JSON"
                else
                  echo "❌ $file - Invalid JSON"
                  exit 1
                fi
              else
                echo "❌ $file - File not found"
                exit 1
              fi
            done
          }
          
          # Function to update app in JSON file
          update_app_in_file() {
            local file=$1
            local app_name=$2
            local bundle_id=$3
            local version=$4
            local download_url=$5
            local tweak_version=$6
            
            echo "🔄 Updating $app_name in $file..."
            
            # Generate localized description with updated versions
            if [[ "$app_name" == "EeveeSpotify" ]]; then
              LOCALIZED_DESC="Tweaked Spotify with premium features unlocked, no ads, and enhanced playback. Spotify v$version, EeveeSpotify $tweak_version."
            elif [[ "$app_name" == "YTLite" ]]; then
              LOCALIZED_DESC="Tweaked YouTube with background playback, no ads, and picture-in-picture. Youtube v$version, YTLite $tweak_version."
            elif [[ "$app_name" == "X (NeoFreeBird)" ]]; then
              LOCALIZED_DESC="Tweaked Twitter/X with premium features and more. X v$version, NeoFreeBird $tweak_version."
            else
              LOCALIZED_DESC=$(jq -r --arg app "$app_name" --arg bundle "$bundle_id" '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)).localizedDescription' "$file")
            fi
            
            # Update based on file structure
            if [[ "$file" == "sidestore.json" ]]; then
              jq --arg app "$app_name" \
                 --arg bundle "$bundle_id" \
                 --arg version "$version" \
                 --arg url "$download_url" \
                 --arg desc "$LOCALIZED_DESC" \
                 --arg date "$CURRENT_DATE" \
                 '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)) |= 
                  .versions[0].version = $version |
                  .versions[0].downloadURL = $url |
                  .versions[0].date = $date |
                  .localizedDescription = $desc' "$file" > "${file}.tmp"
                  
            elif [[ "$file" == "feather.json" ]]; then
              jq --arg app "$app_name" \
                 --arg bundle "$bundle_id" \
                 --arg version "$version" \
                 --arg url "$download_url" \
                 --arg desc "$LOCALIZED_DESC" \
                 --arg date "$CURRENT_DATE" \
                 '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)) |= 
                  .version = $version |
                  .downloadURL = $url |
                  .versionDate = $date |
                  .size = (.versions[0].size) |
                  .versions[0].version = $version |
                  .versions[0].downloadURL = $url |
                  .versions[0].date = $date |
                  .localizedDescription = $desc' "$file" > "${file}.tmp"
                  
            else  # trollapps.json
              jq --arg app "$app_name" \
                 --arg bundle "$bundle_id" \
                 --arg version "$version" \
                 --arg url "$download_url" \
                 --arg desc "$LOCALIZED_DESC" \
                 '(.apps[] | select(.name == $app or .bundleIdentifier == $bundle)) |= 
                  .version = $version |
                  .downloadURL = $url |
                  .localizedDescription = $desc' "$file" > "${file}.tmp"
            fi
            
            mv "${file}.tmp" "$file"
          }

          # Validate files first
          validate_json
          
          UPDATED_APPS=()
          SELECTED_APPS="${{ inputs.apps_to_update }}"
          
          # Update apps based on selection
          if [[ "$SELECTED_APPS" == *"spotify"* ]]; then
            UPDATED_APPS+=("EeveeSpotify")
            SPOTIFY_VER=$(parse_version "${{ inputs.spotify_version }}")
            SPOTIFY_TWEAK=$(parse_tweak_version "${{ inputs.spotify_version }}")
            for file in sidestore.json feather.json trollapps.json; do
              update_app_in_file "$file" "EeveeSpotify" "com.spotify.client" \
                "$SPOTIFY_VER" \
                "${{ inputs.spotify_url }}" \
                "$SPOTIFY_TWEAK"
            done
          fi

          if [[ "$SELECTED_APPS" == *"youtube"* ]]; then
            UPDATED_APPS+=("YTLite")
            YOUTUBE_VER=$(parse_version "${{ inputs.youtube_version }}")
            YOUTUBE_TWEAK=$(parse_tweak_version "${{ inputs.youtube_version }}")
            for file in sidestore.json feather.json trollapps.json; do
              update_app_in_file "$file" "YTLite" "com.google.ios.youtube" \
                "$YOUTUBE_VER" \
                "${{ inputs.youtube_url }}" \
                "$YOUTUBE_TWEAK"
            done
          fi

          if [[ "$SELECTED_APPS" == *"twitter"* ]]; then
            UPDATED_APPS+=("X (NeoFreeBird)")
            TWITTER_VER=$(parse_version "${{ inputs.twitter_version }}")
            TWITTER_TWEAK=$(parse_tweak_version "${{ inputs.twitter_version }}")
            for file in sidestore.json feather.json trollapps.json; do
              update_app_in_file "$file" "X (NeoFreeBird)" "com.atebits.Tweetie2" \
                "$TWITTER_VER" \
                "${{ inputs.twitter_url }}" \
                "$TWITTER_TWEAK"
            done
          fi

          echo "updated_apps=${UPDATED_APPS[*]}" >> $GITHUB_OUTPUT
          
          # Verify changes after updates
          echo "✅ Verifying updated files..."
          for file in sidestore.json feather.json trollapps.json; do
            if jq empty "$file" 2>/dev/null; then
              echo "✅ $file - Still valid after updates"
            else
              echo "❌ $file - Became invalid after updates"
              exit 1
            fi
          done

      - name: "📝 Commit and push changes"
        if: steps.update-files.outputs.updated_apps
        run: |
          # Generate commit message
          if [[ -n "${{ inputs.change_description }}" ]]; then
            COMMIT_MSG="${{ inputs.change_description }}"
          else
            COMMIT_MSG="🔄 Repository Sync: Updated app versions across all sources"
            
            UPDATED_APPS_ARR=(${UPDATED_APPS[@]})
            if [ ${#UPDATED_APPS_ARR[@]} -gt 0 ]; then
              COMMIT_MSG+="\n\nUpdated apps:"
              for app in "${UPDATED_APPS_ARR[@]}"; do
                COMMIT_MSG+="\n• $app"
              done
            fi
            
            COMMIT_MSG+="\n\n🤖 Automated update via GitHub Actions"
          fi
          
          # Configure git and commit
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add sidestore.json feather.json trollapps.json
          
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "$(echo -e "$COMMIT_MSG")"
            git push
            echo "✅ Changes committed and pushed successfully"
          fi

      - name: "📊 Update summary"
        run: |
          echo "## 🎯 Repository Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📱 Apps Updated:" >> $GITHUB_STEP_SUMMARY
          if [ -n "${{ steps.update-files.outputs.updated_apps }}" ]; then
            for app in ${{ steps.update-files.outputs.updated_apps }}; do
              echo "- 🎯 $app" >> $GITHUB_STEP_SUMMARY
            done
          else
            echo "ℹ️ No apps were updated" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📁 Files Updated:" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ sidestore.json" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ feather.json" >> $GITHUB_STEP_SUMMARY  
          echo "- ✅ trollapps.json" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Automated repository management* 🤖" >> $GITHUB_STEP_SUMMARY