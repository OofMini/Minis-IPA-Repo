name: Cleanup Workflow Runs

on:
  workflow_dispatch:
    inputs:
      clear_app_version_updates:
        description: "Clear 'App Version Updates'"
        type: boolean
        default: true
      
      clear_bulk_updates:
        description: "Clear 'Bulk IPA Updates'"
        type: boolean
        default: true
      
      clear_pages:
        description: "Clear 'pages-build-deployment'"
        type: boolean
        default: true

      retain_latest:
        description: "Keep the latest run for each?"
        type: boolean
        default: true

permissions:
  actions: write
  contents: read

jobs:
  delete-runs:
    name: "Delete Old Workflow Runs"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: "Delete Workflow Runs"
        env:
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLEAR_APP: ${{ github.event.inputs.clear_app_version_updates }}
          CLEAR_BULK: ${{ github.event.inputs.clear_bulk_updates }}
          CLEAR_PAGES: ${{ github.event.inputs.clear_pages }}
          RETAIN_LATEST: ${{ github.event.inputs.retain_latest }}
        run: |
          # Use +e to prevent exit on minor errors during loops
          set +e

          delete_runs() {
            local workflow_file="$1"
            local toggle="$2"
            local pretty_name="$3"
            
            if [[ "$toggle" != "true" ]]; then
              echo "⏭️ Skipping '$pretty_name' ($workflow_file)"
              return
            fi
            
            echo "🔍 Processing '$pretty_name'..."
            
            # Loop to handle pagination and deletion
            local loop_count=0
            local max_loops=50 # Safety limit
            
            while [ $loop_count -lt $max_loops ]; do
              # Fetch runs: Completed/Failed only. Using explicit JSON parsing.
              echo "   fetching run list for $workflow_file..."
              
              # NOTE: We use the FILENAME ($workflow_file) not the name for better reliability
              RUNS=$(gh run list --workflow "$workflow_file" --limit 30 --json databaseId,status --status completed,cancelled,failure,skipped,timed_out 2>/dev/null)
              
              if [[ -z "$RUNS" ]] || [[ $(echo "$RUNS" | jq length) -eq 0 ]]; then
                echo "   ✅ No cleanable runs found."
                break
              fi
              
              # Logic to preserve the latest run
              if [[ "$RETAIN_LATEST" == "true" ]]; then
                COUNT=$(echo "$RUNS" | jq length)
                
                # If only 1 run exists and we want to keep it, stop.
                if [[ "$COUNT" -eq 1 ]]; then
                   echo "   🛡️ Latest run preserved. Cleanup complete for this workflow."
                   break
                fi
                
                # Exclude the first one (index 0) from the deletion list
                IDS=$(echo "$RUNS" | jq -r '.[1:] | .[].databaseId')
              else
                # Delete all
                IDS=$(echo "$RUNS" | jq -r '.[].databaseId')
              fi
              
              # Check if we have IDs to delete (handle empty string from jq)
              if [[ -z "$IDS" ]]; then
                 echo "   ✨ No runs to delete in this batch."
                 break
              fi
              
              # Calculate count for logging
              BATCH_COUNT=$(echo "$IDS" | wc -w)
              echo "   🗑️ Deleting batch of $BATCH_COUNT runs..."
              
              # Batch delete
              echo "$IDS" | xargs -n 20 gh run delete 
              
              ((loop_count++))
              sleep 2 # Pause to respect API rate limits
            done
            
            echo "----------------------------------------"
          }
          
          echo "🧹 Starting Cleanup..."
          echo "----------------------------------------"
          
          # Pass: FILENAME, TOGGLE, DISPLAY_NAME
          delete_runs "app-version-updates.yml" "$CLEAR_APP" "App Version Updates"
          delete_runs "bulk-tweaked-apps-updates.yml" "$CLEAR_BULK" "Bulk IPA Updates"
          delete_runs "pages-build-deployment" "$CLEAR_PAGES" "Pages Deployment"
          
          echo "🎉 Cleanup finished."
