name: Cleanup Workflow Runs

on:
  workflow_dispatch:
    inputs:
      clear_app_version_updates:
        description: "Clear 'App Version Updates'"
        type: boolean
        default: true
      
      clear_bulk_updates:
        description: "Clear 'Bulk IPA Updates'"
        type: boolean
        default: true
      
      clear_pages:
        description: "Clear 'pages-build-deployment'"
        type: boolean
        default: true

      retain_latest:
        description: "Keep the latest run for each?"
        type: boolean
        default: true

permissions:
  actions: write
  contents: read

jobs:
  delete-runs:
    name: "Delete Old Workflow Runs"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    
    steps:
      - name: "Delete Workflow Runs"
        env:
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLEAR_APP: ${{ github.event.inputs.clear_app_version_updates }}
          CLEAR_BULK: ${{ github.event.inputs.clear_bulk_updates }}
          CLEAR_PAGES: ${{ github.event.inputs.clear_pages }}
          RETAIN_LATEST: ${{ github.event.inputs.retain_latest }}
        run: |
          set -e

          delete_runs() {
            local workflow_name="$1"
            local toggle="$2"
            
            if [[ "$toggle" != "true" ]]; then
              echo "⏭️ Skipping '$workflow_name'"
              return
            fi
            
            echo "🔍 Processing '$workflow_name'..."
            
            # Loop until no runs remain (handling pagination manually)
            while true; do
              # Fetch runs (Limit 1000 per page to maximize throughput)
              RUNS=$(gh run list --workflow "$workflow_name" --limit 1000 --json databaseId,status || true)
              
              # Check if runs were returned
              if [[ -z "$RUNS" ]] || [[ $(echo "$RUNS" | jq length) -eq 0 ]]; then
                echo "✅ No (more) runs found for '$workflow_name'."
                break
              fi
              
              # Extract IDs based on retention policy
              if [[ "$RETAIN_LATEST" == "true" ]]; then
                # Drop the first item (latest run)
                IDS=$(echo "$RUNS" | jq -r '.[1:] | .[].databaseId')
                
                # If IDs is empty, it means only 1 run existed (the one we kept)
                if [[ -z "$IDS" ]]; then
                  echo "🛡️ Latest run preserved. Cleanup complete."
                  break
                fi
              else
                # Get ALL IDs
                IDS=$(echo "$RUNS" | jq -r '.[].databaseId')
              fi
              
              COUNT=$(echo "$IDS" | wc -w)
              echo "   🗑️ Deleting batch of $COUNT runs..."
              
              # Batch delete using xargs:
              # -n 50: Pass 50 IDs per command call (Reduces API overhead)
              # -P 5:  Run 5 delete commands in parallel (Increases speed)
              echo "$IDS" | xargs -n 50 -P 5 gh run delete || true
              
            done
            echo "----------------------------------------"
          }
          
          echo "🧹 Starting Fast Cleanup..."
          echo "----------------------------------------"
          
          # 1. App Version Updates
          delete_runs "App Version Updates" "$CLEAR_APP"
          
          # 2. Bulk IPA Updates
          delete_runs "Bulk IPA Updates - Tweaked Apps" "$CLEAR_BULK"
          
          # 3. Pages Build and Deployment
          delete_runs "pages-build-deployment" "$CLEAR_PAGES"
          
          echo "🎉 All requested cleanups finished."
